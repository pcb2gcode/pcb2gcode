{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "gerbv Git tag built from source in CI (GERBV_GIT_TAG)",
      "managerFilePatterns": ["/^\\.github/workflows/ci\\.yml$/"],
      "matchStrings": [
        "GERBV_GIT_TAG:\\s+(?<currentValue>v\\d+\\.\\d+\\.\\d+)\\s"
      ],
      "datasourceTemplate": "github-tags",
      "packageNameTemplate": "gerbv/gerbv",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "GEOS Git tag built from source in CI (matrix geos anchor)",
      "managerFilePatterns": ["/^\\.github/workflows/ci\\.yml$/"],
      "matchStrings": [
        "-\\s+&geos_version\\s+'(?<currentValue>\\d+\\.\\d+\\.\\d+)'"
      ],
      "datasourceTemplate": "github-tags",
      "packageNameTemplate": "libgeos/geos",
      "versioningTemplate": "semver"
    }
  ]
}
